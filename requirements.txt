# Shadow AI Detector — Full Python Dependencies (incl. Presidio/spaCy NLP)
#
# presidio-analyzer / presidio-anonymizer / spacy are 2024-era pins whose
# build chain (thinc -> blis -> Cython) can break against whatever numpy
# PyPI resolves today, forcing a source build that may fail depending on
# your platform/toolchain (this has been observed on both Windows+recent-
# Python and in CI). If `pip install -r requirements.txt` fails on this
# chain, use requirements-deploy.txt instead — the app runs fully
# functional on the regex fallback without Presidio/spaCy at all (see
# SECURITY.md "PII Detection Coverage"). CI's blocking test gate uses
# requirements-deploy.txt for exactly this reason; this full file is
# covered by CI's advisory (non-blocking) job only.
#
# Install:
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_lg   # Presidio NLP model
#
# Minimum Python: 3.11

# ── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0           # [standard] includes uvloop + httptools

# ── Data Validation ──────────────────────────────────────────────────────────
pydantic==2.7.1

# ── PII Detection (Microsoft Presidio) ───────────────────────────────────────
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.4                        # NLP engine used by Presidio

# ── Terminal Output ───────────────────────────────────────────────────────────
rich==13.7.1

# ── Cryptography (TLS-readiness, NIST SC-13) ─────────────────────────────────
cryptography==42.0.7

# ── Structured Logging ───────────────────────────────────────────────────────
python-json-logger==2.0.7

# ── Environment / Config ─────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                       # async test client for FastAPI
