# Shadow AI Detector — Python Dependencies
# All packages verified against PyPI (May 2026).
# Pinned to minor version for reproducibility; patch updates are safe.
#
# Install:
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_lg   # Presidio NLP model
#
# Minimum Python: 3.11

# ── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0           # [standard] includes uvloop + httptools

# ── Data Validation ──────────────────────────────────────────────────────────
pydantic==2.7.1

# ── PII Detection (Microsoft Presidio) ───────────────────────────────────────
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.4                        # NLP engine used by Presidio

# ── Terminal Output ───────────────────────────────────────────────────────────
rich==13.7.1

# ── Cryptography (TLS-readiness, NIST SC-13) ─────────────────────────────────
cryptography==42.0.7

# ── Structured Logging ───────────────────────────────────────────────────────
python-json-logger==2.0.7

# ── Environment / Config ─────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                       # async test client for FastAPI
