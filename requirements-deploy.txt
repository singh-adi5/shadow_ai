# Shadow AI Detector — Cloud Deployment Dependencies
# Lightweight build: no spaCy/Presidio (regex fallback active in demo mode)
# Regex fallback detects: CREDIT_CARD, EMAIL_ADDRESS, US_SSN, API_KEY,
#                         GENERIC_PASSWORD, PHONE_NUMBER, IBAN_CODE
#
# To enable full Presidio ML detection (requires ~1.5 GB RAM):
#   pip install presidio-analyzer presidio-anonymizer
#   python -m spacy download en_core_web_lg

fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
redis==5.0.4
python-json-logger==2.0.7
cryptography==42.0.7
